import { Routes } from '@angular/router';
import { LandingComponent } from '../layout/landing-component/landing-component';

export const routes: Routes = [{ path: '', component: LandingComponent }];
